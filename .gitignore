# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/
.vscode
.reliably
*.bak

*.sarif
*.sarif.json
output.txt
output2.txt

# this is the default output of the `reliably report` command
report.json

# prevent reliably.yaml files being committed
reliably.yaml

# ignore delve(dlv) debug directory
__debug_bin
