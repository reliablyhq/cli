package metrics

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

// Define a mock it for latency and parsestatus and test
// Might have to iterate over "it" and create a map with the "resp"s that then becomes what is fed to the functions in order to test.

func TestParseLatency(t *testing.T) {
	assert.Equal(t, nil, nil, "Unexpected error")
}

func TestParseStatusErrors(t *testing.T) {
	assert.Equal(t, nil, nil, "Unexpected error")
}
